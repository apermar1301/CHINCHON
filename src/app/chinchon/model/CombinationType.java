package app.chinchon.model;

/**
 * Tipos de combinacion validos en el chinchon.
 */
public enum CombinationType {

    /** Cartas del mismo valor con distintos palos. */
    GROUP,

    /** Cartas consecutivas del mismo palo. */
    RUN
}
